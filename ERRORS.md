# ERRORS.md — Purama AI

| DATE | BUG | CAUSE | FIX |
|------|-----|-------|-----|
| 2026-07-25 | Edge functions manquent validation Zod | Aucune validation stricte requêtes AI | Ajout ChatMessageSchema + AgentRunSchema + validateBody() dans _shared/validation.ts, appliqué à 10 fonctions AI critiques |
| 2026-07-25 | Edge functions sans rate limiting | Abus possible endpoints AI (coût Claude API) | Ajout rate-limit.ts in-memory (Deno-compatible), 20 req/h chat, 10 req/h agent-run, 30 req/h chatbot public |
| 2026-07-25 | Boutons Google OAuth sans aria-label | A11y WCAG violation | Ajout aria-label + aria-hidden sur SVG dans Login.tsx + Signup.tsx |
| 2026-07-25 | Toggle comparaison pricing sans aria-expanded | A11y WCAG violation | Ajout aria-label + aria-expanded dans PricingSection.tsx |
| 2026-07-25 | App React sans ErrorBoundary | Crash non géré → écran blanc | Ajout react-error-boundary wrapping <Routes>, fallback FR avec bouton reload |
| 2026-07-26 | `chat`/`chatbot` edge functions 500 "LOVABLE_API_KEY is not configured" | Scaffold Lovable.dev jamais migré, clé jamais configurée sur le VPS self-hosted | Migration vers Anthropic direct (`_shared/anthropic-stream.ts`, traduction SSE Anthropic→format OpenAI pour 0 impact frontend) |
| 2026-07-26 | Marketplace agents 100% vide + `agent-proxy` 404 sur tout agent | `purama_ai.agents` avait 0 lignes (jamais seedée) | `karta/scripts/reseed-agents.ts` — 45 agents réels avec webhook_slug reconstruits depuis n8n `webhook_entity` (plusieurs slugs de `agentConfigs.ts` étaient morts) |
| 2026-07-26 | Déploiement `chat`/`chatbot` crash "Module not found _shared/rate-limit.ts" | `supabase/functions/_shared/` sur le VPS désynchronisé du repo depuis le 2026-07-25 (fixes sécurité jamais déployées) | Redéploiement complet de `supabase/functions/` sur le VPS + restart `supabase-edge-functions` |
| 2026-07-26 | KARTA Docker crash "Cannot find module dist/index.js" | `tsconfig.json` avec `rootDir:"."` incluant scripts/+test/ → sortie compilée sous `dist/src/` au lieu de `dist/` | `tsconfig.build.json` dédié (`rootDir:"src"`, `include: src/**`) utilisé uniquement pour `npm run build` |
| 2026-07-26 | KARTA Docker crash "Node.js 20 detected without native WebSocket support" (supabase-js realtime) | Image `node:20-alpine` — realtime-js exige WebSocket natif (Node 22+) même sans usage réel de Realtime | Dockerfile passé sur `node:22-alpine` |
| 2026-07-26 | API KARTA 404 "Route inconnue" sur `/trigger/crm-intelligent/:userId` et kill-switch action agents | Regex routes `([a-z]+)` ne matche pas les slugs à tiret (`crm-intelligent`, `machine-de-suivi`...) | Regex passées à `([a-z-]+)` dans `src/api/server.ts` |
| 2026-07-26 | Marketplace agents affichait texte brut ("Users", "Target"...) au lieu d'icônes | `agents-catalog.ts` (reseed Phase 0) utilisait des noms de composants Lucide alors que `DashboardAgents.tsx`/`AgentDetail.tsx` rendent `agent.icon` en texte brut littéral (fallback `'🤖'`) | 45 icônes remplacées par emoji littéraux, re-reseed + vérifié en SQL prod |
| 2026-07-26 | `npm run build` (Vite) crash "Error: The service was stopped" / esbuild+@swc/core natifs KO | macOS Gatekeeper : binaires `.node` téléchargés par npm marqués `com.apple.quarantine`, "code signature not valid for use in process" (esbuild, @swc/core-darwin-arm64, fsevents) | `xattr -dr com.apple.quarantine node_modules/` (fix machine locale, à refaire après tout `npm install` complet si ça revient) |
| 2026-07-26 | SSH vers le VPS (72.62.191.111) "Connection refused" en plein déploiement Phase 4 — bloque le déploiement de `karta-trigger` + rebuild `karta-engine` | Probable lockout fail2ban : une 1ère tentative `scp` sans `sshpass` (auth par clé, aucune clé locale valide) a généré 6 échecs d'authentification rapides, motif typique de brute-force | Non résolu en session — nécessite soit d'attendre l'expiration du ban, soit un déblocage manuel côté VPS (console Hostinger : `fail2ban-client unban <IP>` ou `fail2ban-client set sshd unbanip <IP>`). TOUJOURS utiliser `sshpass -p ... ssh/scp` dès la 1ère tentative, jamais `scp`/`ssh` nu (évite l'auth par clé qui échoue et déclenche le ban) |
| 2026-07-27 | Exigence brief "non négociable" (§Sécurité) jamais implémentée : rate limit anti-ban Gmail (max 400 envois/jour/compte) | `gmailSendTool` envoyait sans aucun plafond quotidien — au niveau d'autonomie 3 (agit seul), un agent aurait pu envoyer en boucle sans limite | Compteur quotidien ajouté (`karta_agent_memory`, clé partagée `agent_type='gmail'` entre `email` et `repondeur-intelligent` — même compte Gmail réel), vérifié par 3 tests unitaires, déployé et healthy |
| 2026-07-26 | Risque de double-traitement identifié AVANT mise en prod (Phase 3, Agent Créateur d'Agents) : un agent créé planifié (`schedule_enabled=true`) activé en exécution réelle (`karta_enabled=true`) aurait pu être exécuté 2× en parallèle — une fois par le scheduler n8n legacy (toujours actif, filtre uniquement sur `schedule_enabled`), une fois par le nouveau scheduler KARTA | Le scheduler n8n existant (`creator-agent-scheduler.json`) ignore `karta_enabled` (n'existait pas à sa création) | Garde ajoutée dans `creator-agent-run` : refuse (409) tout appel `trigger="cron"` si `karta_enabled=true` — vérifié réel via curl (service-role + trigger cron → 409) |
| 2026-07-27 | QA-agent : DEPLOY BLOQUÉ — 4 bloquants sur KARTA/Phase 1-4 | (1) Validation humaine (autonomie niveau 1/2) marquait l'action "en attente" mais ne l'exécutait JAMAIS après approbation — dead-end complet, contredisait la promesse UI "tu valides toujours avant". (2) Agents créés (`custom:*`) n'avaient aucun toggle `simulation_mode` → restaient éternellement en dry-run malgré le texte "fait vraiment les actions". (3) Hooks React Query KARTA n'exposaient jamais `isError` → écrans silencieusement faux ("0 actif sur 0") en cas d'erreur RLS/réseau. (4) Erreurs anglaises dans `creator-agent-run`/`chat`/`chatbot` (+ incohérence de marque "AI Agents Pro" vs "Purama AI") | (1) Nouvelle table `karta_pending_actions` (migration 005) + `engine/approval.ts` (`resolvePendingAction` : approve = résout l'AgentDefinition et EXÉCUTE réellement l'outil en mode live, reject = jamais exécuté) + routes API `/pending-actions/:id/(approve\|reject)` + edge function proxy `karta-resolve-pending-action` (vérif propriété RLS) + UI Approuver/Rejeter dans `MyEmployees.tsx` et `CreatorAgentDetail.tsx`. Vérifié RÉEL sur le VPS : approve → vraie ligne `compta_transactions` créée, `karta_runs.status` patché en `success`, double-approve → 400 "Action déjà traitée" ; reject → outil jamais appelé, aucune ligne créée. (2) Toggle `simulation_mode` ajouté dans `CreatorAgentDetail.tsx` (section Exécution réelle KARTA), même pattern que `MyEmployees.tsx`. (3) `usePendingActions`/`useKartaStats` exposent `isError`, pages affichent un message FR explicite au lieu de masquer l'erreur. (4) Tous les messages traduits + marque uniformisée sur "Purama AI" |
| 2026-07-27 | Tokens OAuth Gmail (`email_agent_config.gmail_access_token`/`gmail_refresh_token`) stockés en clair depuis avril — brief §Phase 3 exige "tokens chiffrés (AES-256)" | Jamais implémenté à la création de `email_agent_config` ; 0 connexion réelle existante (0/2 lignes avec token), migration sans risque | AES-256-GCM (iv+ciphertext+tag concaténés en base64), `karta/src/lib/gmail-token-crypto.ts` (Node) + `supabase/functions/_shared/gmail-token-crypto.ts` (Deno, Web Crypto), même layout binaire pour interop cross-runtime. `GMAIL_TOKEN_ENCRYPTION_KEY` (32 octets base64) déployée sur le VPS (containers `functions` + `karta-engine`). Vérifié réel : chiffré côté Deno → déchiffré côté Node avec la vraie clé de prod |
| 2026-07-27 | Bug réel trouvé via test Playwright réel (jamais testé en navigateur avant) : la modale d'onboarding "Embauche ton 1er employé IA" restait VIDE après une embauche réussie, au lieu d'afficher le résultat | `usePollLatestRun(step === 'working' && !pollTimedOut ? selected : null, sinceIso)` — gater `agentType` sur `step` change la queryKey React Query pile au moment où `step` passe à `'result'`, réinitialisant `run.data` à `undefined` (nouvelle query désactivée) avant même que la condition de rendu `run.data &&` ne s'évalue | `agentType` passé sans condition sur `step` (`usePollLatestRun(selected, sinceIso)`) — le hook stoppe déjà lui-même le polling une fois la donnée trouvée (`refetchInterval` conditionnel), pas besoin de désactiver la query via un changement de clé. Vérifié réel : test Playwright passe, résultat affiché avec le badge TODO_LIVE_TEST |
| 2026-07-27 | Bug réel trouvé via audit Lighthouse (accessibilité 95→100, `button-name` sur un bouton sans nom accessible) : la page d'accueil montrait 2 widgets de chat flottants superposés | `src/pages/Index.tsx` montait `<Chatbot />` (composant legacy dupliqué, jamais retiré) EN PLUS de `<ChatbotWidget />` déjà monté globalement dans `App.tsx` pour toutes les routes — le composant legacy n'avait jamais reçu le fix a11y (`ae83eb3`/`d7bd61c`) car personne ne savait qu'il était encore utilisé | `<Chatbot />` retiré de `Index.tsx`, fichier `src/components/Chatbot.tsx` supprimé (orphelin après retrait de son unique usage). Accessibilité 100/100 vérifiée réelle après redeploy |
| 2026-07-27 | `SUPABASE_SERVICE_ROLE_KEY` et `DOCUSEAL_API_TOKEN` réels committés en dur dans `tests/docuseal-e2e.spec.ts` depuis `d75db81` (secret de prod partagé par tout l'écosystème Purama, exposé dans l'historique git) | Fallback `?? '...'` ajouté pour que `npx playwright test` fonctionne sans configurer l'environnement — a eu pour effet de committer une vraie clé service_role | Fallback supprimé, la valeur doit venir de l'environnement (throw explicite sinon). **Rotation de la clé recommandée** (décision à prendre séparément — clé partagée par les 21 apps de l'écosystème, hors scope de cette session) |
| 2026-07-27 | `npx playwright test` (config par défaut) tentait d'exécuter `tests/docuseal-e2e.spec.ts` et `tests/karta/**` (config dédiées, prod + env vars requises) — masqué jusqu'ici par le secret en dur ci-dessus | `playwright.config.ts` n'avait aucun `testIgnore` pour exclure les suites à config dédiée | `testIgnore: ['**/docuseal-e2e.spec.ts', '**/karta/**']` ajouté à `playwright.config.ts` |
| 2026-07-27 | **CRITIQUE — BLOQUANT INFRA (pas fixable en SSH)** : `check-subscription` et `create-checkout` ne répondent JAMAIS en prod (timeout total, aucune erreur loggée) — DÉCOUVERT en testant la refonte des plans Pricing. Cause réelle isolée par diagnostic réseau direct (fonction jetable `_diag-net`, supprimée après tests) : le conteneur `supabase-edge-functions` peut résoudre le DNS de n'importe quel host (`Deno.resolveDns` OK) mais un `Deno.connect(host,443)` brut time-out pour TOUTE IP réellement externe (`api.stripe.com`, `api.anthropic.com`, `httpbin.org`, `1.1.1.1`, `google.com`, `purama-ai.purama.dev` sur Vercel) alors qu'il RÉUSSIT vers des hosts hébergés chez le même fournisseur (`n8n.srv1286148.hstgr.cloud`, `docuseal.purama.dev`, `auth.purama.dev`). `iptables`/`nft` sur le VPS ne montrent AUCUNE règle DROP/REJECT pertinente (les 3 règles nftables `raw PREROUTING` trouvées ont 0 paquet compté — non déclenchées, non responsables). Conclusion : filtrage réseau en amont du système d'exploitation invité — très probablement le pare-feu Cloud/VPS Hostinger (hPanel), qui autorise le trafic intra-réseau Hostinger mais bloque par défaut la sortie vers l'Internet public. **Impact réel** : TOUT appel sortant depuis une edge function vers un service externe échoue silencieusement — Stripe (paiements/webhooks/portail), Resend (emails transactionnels — confirmé bloqué aussi), et l'appel direct Anthropic ("migration" du 2026-07-26, jamais vérifié avec un vrai appel car masqué par le manque de crédit) seront TOUS bloqués tant que ce pare-feu n'est pas ouvert. | Filtrage réseau au niveau infrastructure Hostinger (hors du système d'exploitation du VPS — pas de `sshpass ssh` possible pour le corriger, aucune règle `iptables`/`nft` locale n'est en cause) | **NON CORRIGÉ — nécessite une action de Tissma dans le panneau Hostinger** (hPanel → VPS → Pare-feu/Firewall : vérifier/désactiver la restriction sortante, ou autoriser port 443 sortant vers Internet + port SMTP si Resend l'utilise). Traité comme 2e règle permanente de ce type (à côté du crédit Anthropic) : documenter et ne jamais re-tester en boucle tant que non résolu côté Hostinger. `check-subscription` a quand même reçu un fix correct mais insuffisant seul : `httpClient: Stripe.createFetchHttpClient()` + `timeout: 10_000` (requis en environnement Deno/edge, le client HTTP Node par défaut de Stripe ne fonctionne pas sous Deno) |
| 2026-07-27 | Test flaky pré-existant "Login — champs vides bloqués" (`tests/forms-validation.spec.ts`), échouait sur mobile uniquement | Le test cliquait `button[type="submit"]` sans jamais fermer le bandeau cookie/intro d'abord (contrairement au test Signup juste en dessous, qui le fait), donc sur mobile le clic était intercepté par l'overlay | Ajout du même bloc "Dismiss cookie consent if present" que le test Signup, avant le clic. Vérifié réel : 3/3 (desktop/tablette/mobile) passent, suite complète 156/156 |
| 2026-07-27 | `/simplify` (règle CLAUDE.md #17, jamais lancé sur les 3 commits KARTA de cette session) — 4 agents (reuse/simplification/efficacité/altitude) sur `git diff HEAD~3...HEAD` | `PendingActionCard` dupliqué avec variations entre `MyEmployees.tsx` et `CreatorAgentDetail.tsx` (markup, signature toast.error différente) ; `formatRelative` dupliqué localement dans `MyEmployees.tsx` ; import de clé Web Crypto Deno refait à chaque appel dans `_shared/gmail-token-crypto.ts` | Composant partagé `src/components/karta/PendingActionsList.tsx` (prop `compact`) utilisé par les 2 pages ; `formatRelative` déplacé dans `src/lib/utils.ts` ; clé Deno mémorisée en module (`cachedKey`). Vérifié : `tsc` 0 erreur, build 0 erreur, 49/49 vitest karta, 18/18 Playwright KARTA (prod), 156/156 Playwright principal |
| 2026-07-27 | Brief "PRICING & OFFRE IRRÉSISTIBLE" — packs de dépassement (+2000 actions = 19€) manquants : un user au quota atteint n'avait aucune option entre rester bloqué et upgrader tout un plan | `getUsageSnapshot()` ne connaissait aucune notion de crédit ponctuel, `create-checkout` ne gérait que `mode:"subscription"` | Table `purama_ai.overage_credits` (migration `20260727020000`, RLS lecture seule owner, écriture réservée service_role) ; `create-checkout` accepte un flag `oneTime` → `mode:"payment"` avec `metadata.type="overage_pack"` ; `stripe-webhook` crédite `overage_credits` (2000 actions, `period_start` = 1er du mois) sur `checkout.session.completed` quand `mode==="payment" && metadata.type==="overage_pack"` ; `usage.ts` ajoute `sumOverageCreditsThisPeriod()` au calcul de `limit`/`percent` ; `DashboardBilling.tsx` affiche la vraie barre de quota (`getUsageSnapshot`, remplace l'ancien `totalThisMonth/100` codé en dur qui était faux pour tout plan ≠ Free) + CTA d'achat visible dès 80% du quota + gestion des redirections `overage_success`/`overage_canceled`. Vérifié : `tsc` 0 erreur, build 0 erreur, 53/53 vitest karta, `deno check` clean sur `stripe-webhook` (npm: resolution error attendue et déjà documentée sur `create-checkout`, non bloquante). **Bloqué en bout de chaîne comme le reste du paiement Stripe par le pare-feu Hostinger déjà documenté ci-dessus (2026-07-27, CRITIQUE — BLOQUANT INFRA)** : code complet, jamais vérifiable de bout en bout avec un vrai paiement tant que ce pare-feu n'est pas ouvert. Nouvelle preuve de la même cause pendant le déploiement de cette tâche : après `docker compose up -d --force-recreate functions`, `create-checkout` répond correctement à OPTIONS (200, ~0.4s) mais `stripe-webhook` reste bloqué jusqu'à `early termination` forcée par le runtime — seule différence de code entre les deux : `stripe-webhook` importe `Resend` depuis `esm.sh` (dépendance externe), que le cold-start doit résoudre sur le réseau après le recreate (cache Deno vidé). Aucune régression de mon changement — même blocage réseau sortant, juste déclenché différemment (résolution de module au lieu d'un appel API) |
| 2026-07-27 | `/simplify` (règle CLAUDE.md #17) sur le diff packs de dépassement — 4 agents (reuse/simplification/efficacité/altitude) | (reuse) `DashboardBilling.tsx` détectait "illimité" via un sentinel `Number.MAX_SAFE_INTEGER` au lieu de réutiliser `isSuperAdmin()`, et affichait `used`/`limit` en nombres bruts au lieu de `formatExecutions()` déjà utilisé ailleurs. (simplification) `create-checkout` dupliquait `customer`/`customer_email`/`line_items` dans les 2 branches du ternaire `oneTime` ; `buyOveragePack` dupliquait la forme exacte de `openCustomerPortal` (setLoading/invoke/redirect/catch/toast) ; `sumOverageCreditsThisPeriod`/`sumPaidInvoicesThisMonth` dupliquaient la même forme "somme une colonne" sans réutiliser l'abstraction `countTable`. (altitude) `oneTime` était un booléen envoyé par le client et jamais vérifié côté serveur — un flag `oneTime=false` avec le price_id du pack (ou l'inverse) n'aurait pas été détecté avant que Stripe lui-même ne rejette l'incompatibilité mode/récurrence ; le webhook créditait un `2000` en dur sans lien nommé vers `OVERAGE_PACK.actions`. | `isSuperAdmin(user?.email)` réutilisé pour `isUnlimited` ; `formatExecutions()` appliqué à `used`/`limit`. `sessionOptions` restructuré en champs communs + spread mode-spécifique. Helper `redirectToStripe()` partagé par `openCustomerPortal`/`buyOveragePack`. Nouveau `sumTable()` (mirroir de `countTable`) factorisant les 2 fonctions de somme. `oneTime` dérivé côté serveur de `priceId === OVERAGE_PACK_PRICE_ID` (le client n'envoie plus ce flag) — ferme la dépendance à un booléen non vérifié. `OVERAGE_PACK_ACTIONS` constante nommée remplace le `2000` en dur dans `stripe-webhook`. **Skips documentés** : (efficacité) double requête `agent_usage` sur `DashboardBilling` — pattern déjà pré-existant sur `DashboardOverview.tsx` (task #32), pas une régression de ce diff, hors scope pour corriger seul ; poll 60s sur `usage-snapshot` — même pattern déjà établi ailleurs au tableau de bord, gardé pour cohérence, mais `invalidateQueries` ajouté sur `overage_success` pour un rafraîchissement immédiat. (altitude) registre de dispatch webhook généralisé (price→handler) — refusé, sur-ingénierie pour 1 seul SKU ponctuel existant (YAGNI) ; hook `useUsageSnapshot()` partagé pour les 3 call-sites de `getUsageSnapshot` — signalé à faible confiance par l'agent lui-même comme pattern pré-existant, hors scope de cette tâche. Re-vérifié après application : `tsc` 0 erreur, build 0 erreur, 53/53 vitest karta, `deno check` clean sur `stripe-webhook` |
